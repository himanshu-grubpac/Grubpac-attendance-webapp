import ApplyLeaveForm from './ApplyLeaveForm.jsx';

/** Apply WFH — type pinned to WFH with the type selector hidden. */
export default function EmployeeApplyWfh() {
  return <ApplyLeaveForm mode="wfh" />;
}
